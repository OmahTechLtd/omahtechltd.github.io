import React from "react";

const CustomModels = () => {
  return (
    <section className="min-h-screen bg-gray-900 text-white flex flex-col items-center justify-center px-6">
      <h1 className="text-3xl font-bold mb-6 bg-gradient-to-r from-green-400 to-blue-500 bg-clip-text text-transparent">
        Custom Models
      </h1>
      <p className="text-gray-300 max-w-2xl text-center">
        We build advanced, production-ready AI solutions tailored to your organization’s needs.  
        This page will include consultation forms and case studies soon.
      </p>
    </section>
  );
};

export default CustomModels;